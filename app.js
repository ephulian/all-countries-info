import { countries } from './api/getData.js';

// console.log(countries.byPopulation());
const searchBar = document.querySelector('.search');

searchBar.addEventListener('scroll', (e) => console.log(e));

searchBar.scrollTop;
